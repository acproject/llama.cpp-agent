[package]
name = "llama-agent-sdk"
version = "0.1.0"
edition = "2021"

[lib]
name = "llama_agent_sdk"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.12", default-features = true, features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
